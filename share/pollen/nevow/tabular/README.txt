Tabular Data Browser/Editor
===========================

Data
    Filtering
    Ordering
    Paging
    Total count
    Empty

Sorting
Table, Columns and Cells
Column UI, i.e. sortable up/down
Titles and corresponding attribute
Actions
Pagination
Navigation (i.e. letter clicker)
UI to change items per page etc
----
Searching

View can inform data model about the columns it actually needs.

Default implementations for i.e. paging a list() or deferred.

----
cms
click on crafts
designer trainers

columns is really part of the model
the view can display a subset of the columns provided by the model
